@page "/profile"
@using QuizFuzz.Client.Services.Api
@using QuizFuzz.Shared.Dtos.Users
@attribute [Authorize]
@inject IUsersApiClient UsersClient

<PageTitle>@AppStrings.T("ProfilePageTitle")</PageTitle>

<MudContainer MaxWidth="MaxWidth.Large" Class="qf-page-shell">
    @if (stats == null)
    {
        <MudPaper Elevation="0" Class="qf-empty-state">
            <MudProgressCircular Indeterminate="true" Size="Size.Large" />
            <MudText Class="qf-muted">@AppStrings.T("Loading")</MudText>
        </MudPaper>
    }
    else
    {
        <MudPaper Elevation="0" Class="qf-hero mb-5">
            <MudStack Row="true" AlignItems="AlignItems.Center" Spacing="4" Class="flex-wrap">
                <MudAvatar Size="Size.Large" Class="qf-page-header-icon">
                    @stats.Username.Substring(0, 1).ToUpperInvariant()
                </MudAvatar>
                <MudStack Spacing="1">
                    <MudText Typo="Typo.h4" Class="qf-section-title">@stats.Username</MudText>
                    <MudText Typo="Typo.body2" Class="qf-muted">@stats.Email</MudText>
                </MudStack>
            </MudStack>
        </MudPaper>

        <MudGrid Spacing="3">
            <MudItem xs="12" sm="6" md="3">
                <MudPaper Elevation="0" Class="qf-stat-card pa-4">
                    <MudStack AlignItems="AlignItems.Center" Spacing="2">
                        <MudIcon Icon="@Icons.Material.Filled.SportsEsports" Size="Size.Large" Color="Color.Primary" />
                        <MudText Typo="Typo.h4">@stats.GamesPlayed</MudText>
                        <MudText Typo="Typo.body2" Class="qf-muted">@AppStrings.T("GamesPlayed")</MudText>
                    </MudStack>
                </MudPaper>
            </MudItem>
            <MudItem xs="12" sm="6" md="3">
                <MudPaper Elevation="0" Class="qf-stat-card pa-4">
                    <MudStack AlignItems="AlignItems.Center" Spacing="2">
                        <MudIcon Icon="@Icons.Material.Filled.EmojiEvents" Size="Size.Large" Color="Color.Warning" />
                        <MudText Typo="Typo.h4">@stats.GamesWon</MudText>
                        <MudText Typo="Typo.body2" Class="qf-muted">@AppStrings.T("GamesWon")</MudText>
                    </MudStack>
                </MudPaper>
            </MudItem>
            <MudItem xs="12" sm="6" md="3">
                <MudPaper Elevation="0" Class="qf-stat-card pa-4">
                    <MudStack AlignItems="AlignItems.Center" Spacing="2">
                        <MudIcon Icon="@Icons.Material.Filled.CheckCircle" Size="Size.Large" Color="Color.Success" />
                        <MudText Typo="Typo.h4">@stats.QuestionsAnswered</MudText>
                        <MudText Typo="Typo.body2" Class="qf-muted">@AppStrings.T("QuestionsAnswered")</MudText>
                    </MudStack>
                </MudPaper>
            </MudItem>
            <MudItem xs="12" sm="6" md="3">
                <MudPaper Elevation="0" Class="qf-stat-card pa-4">
                    <MudStack AlignItems="AlignItems.Center" Spacing="2">
                        <MudIcon Icon="@Icons.Material.Filled.Speed" Size="Size.Large" Color="Color.Info" />
                        <MudText Typo="Typo.h4">@GetAverageTimeDisplay()</MudText>
                        <MudText Typo="Typo.body2" Class="qf-muted">@AppStrings.T("AverageAnswerTime")</MudText>
                    </MudStack>
                </MudPaper>
            </MudItem>

            <MudItem xs="12" md="6">
                <MudPaper Elevation="0" Class="qf-panel pa-5">
                    <MudText Typo="Typo.h6" Class="mb-4">@AppStrings.T("WinRate")</MudText>
                    <MudProgressLinear Value="@stats.WinRate" Size="Size.Large" Color="Color.Success" Class="mb-3" />
                    <MudText Typo="Typo.h5" Align="Align.Center" Color="Color.Success">@stats.WinRate.ToString("F1")%</MudText>
                </MudPaper>
            </MudItem>

            <MudItem xs="12" md="6">
                <MudPaper Elevation="0" Class="qf-panel pa-5">
                    <MudText Typo="Typo.h6" Class="mb-4">@AppStrings.T("Accuracy")</MudText>
                    <MudProgressLinear Value="@stats.Accuracy" Size="Size.Large" Color="Color.Primary" Class="mb-3" />
                    <MudText Typo="Typo.h5" Align="Align.Center" Color="Color.Primary">@stats.Accuracy.ToString("F1")%</MudText>
                </MudPaper>
            </MudItem>

            <MudItem xs="12">
                <MudPaper Elevation="0" Class="qf-panel pa-5">
                    <MudStack Row="true" AlignItems="AlignItems.Center" Spacing="2" Class="mb-4">
                        <MudIcon Icon="@Icons.Material.Filled.WorkspacePremium" Color="Color.Warning" />
                        <MudText Typo="Typo.h6">@AppStrings.T("Achievements")</MudText>
                    </MudStack>
                    <MudGrid>
                        @if (stats.GamesPlayed >= 1)
                        {
                            <MudItem xs="6" sm="4" md="3"><MudPaper Elevation="0" Class="qf-player-row"><MudStack AlignItems="AlignItems.Center" Spacing="1"><MudIcon Icon="@Icons.Material.Filled.Star" Color="Color.Warning" /><MudText Typo="Typo.caption" Align="Align.Center">@AppStrings.T("FirstGame")</MudText></MudStack></MudPaper></MudItem>
                        }
                        @if (stats.GamesWon >= 1)
                        {
                            <MudItem xs="6" sm="4" md="3"><MudPaper Elevation="0" Class="qf-player-row"><MudStack AlignItems="AlignItems.Center" Spacing="1"><MudIcon Icon="@Icons.Material.Filled.EmojiEvents" Color="Color.Warning" /><MudText Typo="Typo.caption" Align="Align.Center">@AppStrings.T("FirstWin")</MudText></MudStack></MudPaper></MudItem>
                        }
                        @if (stats.QuestionsAnswered >= 10)
                        {
                            <MudItem xs="6" sm="4" md="3"><MudPaper Elevation="0" Class="qf-player-row"><MudStack AlignItems="AlignItems.Center" Spacing="1"><MudIcon Icon="@Icons.Material.Filled.Quiz" Color="Color.Info" /><MudText Typo="Typo.caption" Align="Align.Center">10 Questions</MudText></MudStack></MudPaper></MudItem>
                        }
                        @if (stats.GamesWon >= 10)
                        {
                            <MudItem xs="6" sm="4" md="3"><MudPaper Elevation="0" Class="qf-player-row"><MudStack AlignItems="AlignItems.Center" Spacing="1"><MudIcon Icon="@Icons.Material.Filled.MilitaryTech" Color="Color.Success" /><MudText Typo="Typo.caption" Align="Align.Center">@AppStrings.T("Veteran")</MudText></MudStack></MudPaper></MudItem>
                        }
                    </MudGrid>
                </MudPaper>
            </MudItem>
        </MudGrid>
    }
</MudContainer>

@code {
    private UserStatsDto? stats;

    protected override async Task OnInitializedAsync()
    {
        stats = await UsersClient.GetMyStatsAsync();
    }

    private string GetAverageTimeDisplay()
    {
        if (stats == null || stats.AverageAnswerTimeMs == 0)
            return AppStrings.T("NotAvailable");

        var seconds = stats.AverageAnswerTimeMs / 1000.0;
        return $"{seconds:F1}s";
    }
}
